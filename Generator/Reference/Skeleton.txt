using System;

namespace Modules.L0.Quantities
{
    /// <summary>
    /// __DESC.
    /// </summary>
    [Serializable]
    public struct __CLASS_NAME : Quantity<__CLASS_NAME>
    {
        /* Fields. */
        private double value;

        /* Public properties. */
__STATIC_PROPS

        /* Constructors. */
        public __CLASS_NAME(double value)
        {
            this.value = value;
        }

        /* Math operators. */
__CASTS

        /* Arithmetic operators. */
__MATH

        /* Public methods. */
        public readonly override string ToString() => value.ToString();

__METHODS
    }
}
